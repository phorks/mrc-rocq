From Stdlib Require Import Strings.String.
From MRC Require Import RefCalc.

Definition x := Model.mkFinalVar "x" 0.
Definition y := Model.mkFinalVar "y" 0.
Definition z := Model.mkFinalVar "z" 0.
Definition t := Model.mkFinalVar "t" 0.
Definition i := Model.mkFinalVar "i" 0.
Definition j := Model.mkFinalVar "j" 0.
Definition k := Model.mkFinalVar "k" 0.
Definition r := Model.mkFinalVar "r" 0.
Definition s := Model.mkFinalVar "s" 0.

(* Definition x₀ := Model.to_initial_var x. *)
(* Definition y₀ := Model.to_initial_var y. *)
(* Definition z₀ := Model.to_initial_var z. *)
(* Definition t₀ := Model.to_initial_var t. *)
(* Definition i₀ := Model.to_initial_var i. *)
(* Definition j₀ := Model.to_initial_var j. *)
(* Definition k₀ := Model.to_initial_var k. *)
